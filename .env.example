# Used by docker compose to configure the db service (compose reads .env automatically).
POSTGRES_PASSWORD=dev
POSTGRES_DB=tasks

# Only needed to run the app directly on the host (e.g. `npm run dev`), not via `docker compose up`.
DATABASE_URL=postgres://postgres:dev@localhost:5432/tasks